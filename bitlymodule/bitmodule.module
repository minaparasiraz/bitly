<?php
/**
* Implementation of hook_theme()
*/
function bitmodule_theme($existing, $type, $theme, $path) {
  return array(
    'show_details' => array(
      'variables' => array('items' => array(), 'title' => '')
    )
  );
}